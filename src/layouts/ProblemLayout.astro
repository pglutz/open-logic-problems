---
import BaseLayout from "./BaseLayout.astro";
import StatusBadge from "../components/StatusBadge.astro";
import AreaTags from "../components/AreaTags.astro";
import ImpactMarks from "../components/ImpactMarks.astro";
import CommentSection from "../components/islands/CommentSection";
import type { Area } from "../lib/areas";

interface CanonicalReference {
  title: string;
  author: string;
  venue?: string;
  year?: number;
  link?: string;
  doi?: string;
}

interface Props {
  id: number;
  name: string;
  status: "open" | "closed" | "claimed-proof-no-consensus";
  area: Area[];
  impact: 1 | 2 | 3;
  canonicalReference: CanonicalReference;
  statementHtml: string;
  definitionsHtml?: string;
  partialResultsHtml?: string;
  additionalReferencesHtml?: string;
  notesHtml?: string;
}

const {
  id,
  name,
  status,
  area,
  impact,
  canonicalReference,
  statementHtml,
  definitionsHtml,
  partialResultsHtml,
  additionalReferencesHtml,
  notesHtml,
} = Astro.props;

const title = `${name} (#${id})`;
---

<BaseLayout title={title}>
  <p class="muted problem-id">Problem #{id}</p>
  <h1>{name}</h1>

  <div class:list={["status-box", "statement-box", `status-${status}`]}>
    <div class="meta-row">
      <StatusBadge status={status} />
      <ImpactMarks impact={impact} />
    </div>
    <div class="statement-body" set:html={statementHtml} />
    <div class="area-row">
      <AreaTags areas={area} />
      <a class="suggest-edit-link" href={`/problems/${id}/edit`}>Suggest an edit</a>
    </div>
  </div>

  <section class="reference">
    <h2>Reference for the problem statement</h2>
    <p>
      {canonicalReference.author}, <em>{canonicalReference.title}</em>
      {canonicalReference.venue && <>, {canonicalReference.venue}</>}
      {canonicalReference.year && <>, {canonicalReference.year}</>}
      {canonicalReference.link && (
        <>
          {" "}
          (<a href={canonicalReference.link}>link</a>)
        </>
      )}
      {canonicalReference.doi && (
        <>
          {" "}
          (DOI: <a href={`https://doi.org/${canonicalReference.doi}`}>{canonicalReference.doi}</a>)
        </>
      )}
    </p>
  </section>

  {
    definitionsHtml && (
      <section>
        <h2>Definitions</h2>
        <div set:html={definitionsHtml} />
      </section>
    )
  }

  {
    partialResultsHtml && (
      <section>
        <h2>Known Partial Results</h2>
        <div set:html={partialResultsHtml} />
      </section>
    )
  }

  {
    notesHtml && (
      <section>
        <h2>Notes</h2>
        <div set:html={notesHtml} />
      </section>
    )
  }

  {
    additionalReferencesHtml && (
      <section>
        <h2>Additional References</h2>
        <div set:html={additionalReferencesHtml} />
      </section>
    )
  }

  <section>
    <h2>Comments</h2>
    <CommentSection problemId={id} client:load />
  </section>
</BaseLayout>

<style>
  .problem-id {
    margin-bottom: 0;
  }
  h1 {
    margin-top: 0.1rem;
  }
  .statement-box {
    margin: 1.25rem 0 2rem;
  }
  .meta-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }
  .statement-body :global(p:last-child) {
    margin-bottom: 0;
  }
  .area-row {
    margin-top: 0.85rem;
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
  }
  .suggest-edit-link {
    margin-left: auto;
    font-size: 0.9rem;
  }
  section {
    margin-bottom: 2rem;
  }
  section h2 {
    font-size: 1.15rem;
    border-bottom: 1px solid var(--color-border);
    padding-bottom: 0.25rem;
  }
</style>

<style is:global>
  /* CommentSection is a React island; its markup is rendered client-side, so
     Astro's scoped-style attribute never reaches it — these rules must be
     global. */
  .comment-list {
    list-style: none;
    margin: 0 0 1.25rem;
    padding: 0;
  }
  .comment {
    padding: 0.75rem 0;
    border-bottom: 1px solid var(--color-border);
  }
  .comment-meta {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    margin-bottom: 0.3rem;
    font-size: 0.85rem;
  }
  .comment-number {
    color: var(--color-text-muted);
  }
  .comment-author {
    font-weight: 600;
  }
  .comment-date {
    color: var(--color-text-muted);
  }
  .comment-body p {
    margin: 0.5rem 0;
  }
  .comment-body p:first-child {
    margin-top: 0;
  }
  .comment-body p:last-child {
    margin-bottom: 0;
  }
  .comment-pending-badge {
    background: var(--color-border);
  }
  .comment-rejected-badge {
    background: var(--color-closed-bg);
    border-color: var(--color-closed-border);
  }
  .comment-form {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
    max-width: 32rem;
  }
  .comment-form input,
  .comment-form textarea {
    font: inherit;
    padding: 0.4rem 0.6rem;
    border-radius: var(--radius-sm);
    border: var(--border-width-thick) solid var(--color-border);
    background: var(--color-bg);
    color: var(--color-text);
  }
  .comment-form button {
    align-self: flex-start;
    font-size: 0.95rem;
    font-weight: 600;
    padding: 0.45rem 1.1rem;
    border-radius: var(--radius-md);
    border: var(--border-width) solid var(--color-link);
    background: var(--color-link);
    color: #fff;
    cursor: pointer;
  }
  .comment-form button:hover {
    opacity: 0.92;
  }
  .comment-form button:disabled {
    opacity: 0.6;
    cursor: default;
  }
</style>
